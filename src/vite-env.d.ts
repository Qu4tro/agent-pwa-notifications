/// <reference types="vite/client" />

// Injected by `vite.config.ts` (and by `vitest.config.ts` for tests) from the
// root package.json version. One source of truth for the app version.
declare const __APP_VERSION__: string
