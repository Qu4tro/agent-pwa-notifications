node_modules/
dist/
.wrangler/
.dev.vars
.agent-notify-pwa.local.json
# The pre-rename name. Old checkouts still have one; keep it out of git.
.agent-dash.local.json
*.log
.DS_Store

# Worktrunk worktrees.
.worktrees/

# The route generator's scratch directory. src/routeTree.gen.ts is the output
# and is tracked; this is not.
.tanstack/

# Local agent tooling: the design skills. Not published.
.claude/

# Local scratch: the brief, the plan, the patch, screenshots. Anything meant
# for publication lives in public_docs/ instead.
docs/
